import { StyleSheet } from "react-native";



export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#1E405A',
        width: 100,
        height: 40,
        borderRadius: 8,
        alignItems: 'center',
        justifyContent: 'center'
    },
    title: {
        color: "#fff",
        textTransform: 'uppercase',
        letterSpacing: 1
    }
})